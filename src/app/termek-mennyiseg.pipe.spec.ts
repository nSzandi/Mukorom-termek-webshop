import { TermekMennyisegPipe } from './termek-mennyiseg.pipe';

describe('TermekMennyisegPipe', () => {
  it('create an instance', () => {
    const pipe = new TermekMennyisegPipe();
    expect(pipe).toBeTruthy();
  });
});
